<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.sh.s3.member.memberDAO">
	<insert id="memberJoin" parameterType="com.sh.s3.member.memberDTO"> 
		insert into member values(#{id},#{pw},#{name},#{email},#{phone})
	</insert>
	
	<delete id="setDelete" parameterType="com.sh.s3.member.memberDTO">
		select * from member
	</delete>
</mapper>



<!-- bankbook code  -->
	<select id="getSelect" parameterType="com.sh.s3.bankbook.BankBookDTO" resultType="com.sh.s3.bankbook.BankBookDTO">
	<!-- bookNumber 타입을 Long으로 받기 위해 -->
		select * from bankbook where bookNumber=#{bookNumber}
	</select>
	
	<select id="getList" resultType="com.sh.s3.bankbook.BankBookDTO">
		select * from bankbook order by bookNumber desc
	</select>
	
